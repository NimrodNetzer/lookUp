# Dependencies
node_modules/

# Environment — NEVER commit this
gateway/.env
gateway/.env.bak
**/.env
**/.env.local
**/.env.bak

# Next.js
dashboard/.next/
dashboard/out/

# Notes — local only, don't commit study content
notes/

# Runtime database — local only
data/

# Distribution artifacts — built outputs, not source
dist/
LookUp/
LookUp.zip
gateway/bundle.cjs

# OS
.DS_Store
Thumbs.db
